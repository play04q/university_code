/*#include<iostream>
#include <stdlib.h>
using namespace std;
int f(int n)
{
	return n = 0 ? 1 : f(n - 1) * n;
}
int main()
{
	int n, sum = 1;
	cin >> m >> n;
	sum = f(m) / (f(m - n) * f(n));
	cout << sum << endl;
	return 0;
}*/